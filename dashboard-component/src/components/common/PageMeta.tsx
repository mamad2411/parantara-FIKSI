const PageMeta = ({
  title,
  description,
}: {
  title: string;
  description: string;
}) => null;

export const AppWrapper = ({ children }: { children: React.ReactNode }) => <>{children}</>;

export default PageMeta;
